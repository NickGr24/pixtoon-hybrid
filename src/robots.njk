---
permalink: robots.txt
eleventyExcludeFromCollections: true
---
{%- if isDemo -%}
# Демо-сборка раздела Hybrid Production.
# Содержит кадры работ Pixtoon, которые ещё не опубликованы официально,
# поэтому индексация закрыта целиком.
User-agent: *
Disallow: /
{%- else -%}
# Боевая сборка: индексация открыта.
# Прежняя версия файла закрывала сайт целиком независимо от режима сборки —
# в комментарии значилось «для боевого сайта не нужен», но генерировался он
# всегда, и выложенный как есть каталог выпал бы из поиска.
User-agent: *
Allow: /
{%- endif %}
